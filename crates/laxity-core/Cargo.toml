[package]
name = "laxity-core"
version = "0.1.0"
edition = "2021"
description = "the laxity cost model as a library"

[dependencies]
laxity-types = { path = "../laxity-types" }
serde = { version = "1", features = ["derive"] }
toml = "1"
